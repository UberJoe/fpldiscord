// Ticket 01 skeleton. The phone-first React SPA (Standings / Waiver History /
// Bet views, hamburger nav, meta.pollAfterMs polling) lands in ticket 05.
const root = document.getElementById("root");
if (root) {
  root.textContent = "fpldiscord — web UI arrives in a later ticket.";
}
